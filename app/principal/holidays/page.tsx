// Principal holidays (/principal/holidays): editable + CSV import + year view.
import { HolidaysView } from "@/components/holidays/holidays-view";

export default function PrincipalHolidaysPage() {
  return <HolidaysView editable />;
}
